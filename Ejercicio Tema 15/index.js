const nombre = "Jonathan";
const apellido = "Rodríguez";

const persona = {
  nombre: nombre,
  apellido: apellido,
};
//console.log(persona);

// localStorage.setItem("persona", JSON.stringify(persona));

// sessionStorage.setItem("persona", JSON.stringify(persona));
// let expires = new Date(Date.now() + 120 * 1000).toUTCString();

// document.cookie = "objetoCookie=persona;expires=" + expires;
